package com.techelevator;

import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;

public class BankAccountTest {
	
	//these tests work when BankAccount is not abstract
	
/*
	BankAccount sut;
	
	@Before
	public void setUp() throws Exception {
		
		sut = new BankAccount();
		
	}

	@Test
	public void testWithdraw() {
		
		
		sut.withdraw(new DollarAmount(100));
		assertEquals(-100,sut.getBalance().getTotalAmountInCents());
		
	}

	@Test
	public void testDeposit() {
		
		sut.deposit(new DollarAmount(100));
		assertEquals(100, sut.getBalance().getTotalAmountInCents());
		
	}
	
	@Test
	public void testTransfer() {
		
		BankAccount sut2 = new BankAccount();
		sut.transfer(sut2, new DollarAmount(100));
		assertEquals(-100,sut.getBalance().getTotalAmountInCents());
		assertEquals(100,sut2.getBalance().getTotalAmountInCents());
		
	}
*/
}
